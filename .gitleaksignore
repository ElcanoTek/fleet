# gitleaks allowlist of confirmed FAKE test fixtures.
#
# Format: one "file:rule:startline" fingerprint per line. Comments must be on
# their OWN line (a trailing "# ..." on a fingerprint line is parsed as part of
# the fingerprint and silently ignored by gitleaks v8, so do not use them).
#
# REGENERATE WITH THE PINNED VERSION. The gitleaks version is pinned in
# .github/workflows/{ci,dev-ci}.yml (GITLEAKS_VERSION) and the rule set differs
# BETWEEN VERSIONS: three fixtures below are invisible to 8.21.2 and detected by
# 8.30.1. A local run on some other build will therefore report a different set,
# and "pruning what looks stale" with the wrong binary deletes entries CI still
# needs. Install the pinned version before touching this file.
#
# The startline is also part of the fingerprint, so ANY edit that inserts or
# removes lines ABOVE a fixture shifts it and silently un-ignores it — the gate
# then fails on a file the change never touched. After editing a file named
# here, re-run `gitleaks dir . --exit-code 1` and update the numbers in the same
# commit.
#
# PREFER NOT ADDING TO THIS FILE. A fixture that must merely exercise a regex
# should be assembled from parts and omit the vendor's structural marker, the
# way internal/redact/redact_test.go's fake* constants are — that keeps both
# gitleaks and GitHub push protection quiet with no entry here at all. (Push
# protection is a separate gate with its own rules: it blocks the push itself,
# and unlike this file it cannot be satisfied by a fingerprint.)
#
# A full security scan confirmed NO real secrets in this repo's tree or history;
# every fingerprint below is a fabricated value. Any NEW or un-ignored finding
# will still fail the gitleaks CI job.

# fake: fabricated "sk-or-v1-..."-shaped string fed to redactSecrets() in
# TestRedactSecretsInLog (asserts the value gets redacted).
internal/agentcore/orchestration_test.go:generic-api-key:695

# fake: fabricated sk- fixture in TestBoundModelOutputToolsGovernsRawAuxiliaryRouteBeforeArtifact
# (#793), fed through output governance to assert the secret is redacted before
# artifact staging. Never a real credential.
internal/agentcore/tool_output_limit_test.go:generic-api-key:155

# fake: a fabricated Fast.io response fixture. The JWT's signature segment is
# the literal "FAKEFAKEsignaturesegmentforTESTfixtureONLYnotarealtoken..." and
# the payload's subject is "example-fake-object"; the test asserts the bearer
# URL is vaulted out of the model context.
internal/agentcore/mcp_fastio_response_test.go:jwt:114
internal/agentcore/mcp_fastio_response_test.go:jwt:117

# fake: fabricated "sk-or-v1-..."-shaped string in TestLoadWithQuotedEnvFromPodman
# (double-quote-stripping test).
internal/config/config_test.go:generic-api-key:1263

# fake: fabricated "sk-or-v1-..."-shaped string in TestLoadWithSingleQuotedEnvFromPodman
# (single-quote-stripping test).
internal/config/config_test.go:generic-api-key:1281

# fake: fabricated fixtures in the redact package's own pattern tests — a
# classic glpat- token whose literal shape is what the rule matches, and the
# "or-novel-abc12345" env-literal registration fixture. (The newer vendor
# fixtures need no entry: see the fake* constants in that file.)
internal/redact/redact_test.go:gitlab-pat:46
internal/redact/redact_test.go:generic-api-key:122

# fake: fabricated Web Push subscription placeholder ("fake-p256dh-refreshed")
# compared in TestUpsertPushSubscription's refresh assertion (#292).
internal/store/push_subscriptions_test.go:generic-api-key:50

# knobs.go registry row: the flagged "secret" is the env-knob NAME
# FLEET_A2A_UNARY_WAIT_SECONDS (generic-api-key entropy false positive).
internal/config/knobs.go:generic-api-key:264
